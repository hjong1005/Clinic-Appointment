// =====================================================================
// web/config.js — frontend configuration.
//
// Leave these BLANK to run in demo mode with sample data (nothing saved).
// Fill them in to connect a real Supabase project (Settings → API).
//
// The anon key is meant to be public — Row-Level Security protects your
// data, so it's safe to commit. The SERVICE-ROLE key is NOT used here and
// must never appear in any browser file.
// =====================================================================

window.SUPABASE_URL  = "";   // e.g. "https://abcdefgh.supabase.co"
window.SUPABASE_ANON = "";   // your anon / public key
